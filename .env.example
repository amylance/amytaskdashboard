# Server-only Supabase credentials (never prefixed with VITE_, never sent to the browser bundle).
# Real values live in Vercel's dashboard -> Project Settings -> Environment Variables.
SUPABASE_URL=https://liwmuwkcsurfgjugdvfm.supabase.co
SUPABASE_ANON_KEY=replace-with-anon-key
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key

# Passphrase checked server-side in /api/login. Set the real value only in Vercel.
DASHBOARD_PASSPHRASE=replace-with-a-real-passphrase
